{
  "$comment": [
    "THE DECLARED SERVICE LEVELS FOR SELF-HEALING. This file is the single source; the L10",
    "playbook describes it in prose and layer-10-audit.ps1 reads it, but neither is",
    "authoritative.",
    "",
    "It exists because the design that retired the seeded-CVE model says the window must be",
    "DECLARED in configuration, not inherited from a default: 'this repository has twice been",
    "bitten by criteria silently adopting a timeout nobody chose'. layer-10-audit.ps1",
    "therefore SKIPs rather than assuming a number when this file is missing or malformed - a",
    "criterion that invents its own pass line is not measuring anything.",
    "",
    "Spec: docs/superpowers/specs/2026-09-05-operationalize-self-healing-design.md sections 4-6."
  ],
  "declaredAt": "2026-09-07",
  "declaredBy": "sponsor",

  "slo": {
    "$comment": [
      "Per source and per severity, NEVER averaged into one number - a blended figure hides",
      "the slice that is not moving, which is the failure the compliance platform was built",
      "to avoid. Values are the spec's section 5 table.",
      "",
      "The clock starts when the finding is CREATED and stops when it is closed. A finding",
      "with no upstream fix does not consume its SLO: it is pending-solution, which holds and",
      "ages without failing, and V10.4 is what stops that state becoming a dumping ground.",
      "",
      "TWO SCALES, BOTH DECLARED. critical/high/medium/low are the CVSS-aligned bands that",
      "Dependabot and code scanning's security_severity_level use. error/warning/note are the",
      "OTHER scale: a code-scanning alert on a non-security rule carries no",
      "security_severity_level, so the audit falls back to rule.severity, which speaks in",
      "those words instead.",
      "",
      "The first run after this model shipped reported 13 findings as 'the policy declares no",
      "SLO for severity note' - refusing to measure what nobody had declared rather than",
      "inventing a number, which is the behaviour we want and also a gap in this file. They",
      "are declared here rather than mapped silently in code, because a band the policy does",
      "not name is precisely the thing a reader needs to be able to see is missing.",
      "",
      "error tracks high; warning and note track the 30-day tier."
    ],
    "days": {
      "critical": 7,
      "high": 7,
      "medium": 30,
      "low": 30,
      "error": 7,
      "warning": 30,
      "note": 30
    }
  },

  "closureLookbackDays": {
    "$comment": [
      "V10.2 asks that every closure be traceable. 'Since the last audit' is not a boundary",
      "this script can read - reports are committed but an audit may be re-run, skipped, or",
      "run twice in an hour - so the window is declared instead. 30 days covers a monthly",
      "compliance cycle with room for a gap, and every closure inside it must carry either a",
      "complete heal trail or an explicit dismissal record."
    ],
    "value": 30
  },

  "laneDeferral": {
    "$comment": [
      "A lane whose MECHANISM does not exist yet, deferred with a DEADLINE rather than",
      "excused indefinitely. Its findings do not count against V10.1 until the date below;",
      "on that date the deferral stops applying by itself, and the criterion goes red again",
      "if the lane still has no mechanism.",
      "",
      "THE EXPIRY IS THE POINT. An exclusion that cannot expire is exactly the dumping",
      "ground V10.4 exists to prevent, one level up. The audit prints the deferred count,",
      "the expiry and the days remaining on EVERY run, and prints DEFERRAL EXPIRED after the",
      "date - so this cannot be quietly forgotten.",
      "",
      "A deadline, not an amnesty. The findings are real, and they stay visible."
    ],
    "container-image": {
      "reason": "Lane 3 has no automated path on this subscription: ACR Tasks returns TasksOperationsNotAllowed (spec section 8, P1 - reproduced twice, on two registries in two resource groups), and the documented scheduled-rebuild fallback is not built yet. Counting base-image findings against a heal SLO states something true and unactionable. SECOND, INDEPENDENT BLOCKER as of 2026-09-17: for the findings actually open in this lane, no patched base image exists to rebuild ONTO - see baseImages.observations. The deferral was about automation; a manual rebuild today would produce the same twelve alerts.",
      "expires": "2026-10-07",
      "unblockedBy": "the scheduled rebuild in spec sections 8 and 11, or an Azure support request granting ACR Tasks",
      "baseImages": {
        "$comment": [
          "THE INVENTORY THIS LANE IS DEFERRED FOR, and the posture of each entry as last",
          "resolved against the upstream registry rather than remembered.",
          "",
          "It exists because a deferral whose SCOPE nobody can read is the dumping ground V10.4",
          "exists to prevent, one level up. 'Container images' is not a scope; these five",
          "Dockerfiles and these three external bases are. A base changed without a line here is",
          "a base whose CVE posture nobody re-checked, and",
          "verification/tests/failure-classes.Tests.ps1 fails until the inventory and the",
          "Dockerfiles agree again.",
          "",
          "'declared' lists every EXTERNAL image an app Dockerfile pulls. Internal stage",
          "references (FROM deps AS build) are not listed: they resolve inside the same file and",
          "reach no registry."
        ],
        "observedAt": "2026-09-17",
        "declared": [
          {
            "image": "node:24-alpine",
            "usedBy": ["apps/compliance/Dockerfile", "apps/control-tower/Dockerfile", "apps/launch-ops/Dockerfile"],
            "role": "build stage only - its filesystem does not reach any published image"
          },
          {
            "image": "nginx:1.31-alpine",
            "usedBy": ["apps/compliance/Dockerfile", "apps/control-tower/Dockerfile", "apps/launch-ops/Dockerfile"],
            "role": "runtime for the three static frontends"
          },
          {
            "image": "node:24-bookworm-slim",
            "usedBy": ["apps/data-api/Dockerfile", "apps/mcp-tools/Dockerfile"],
            "role": "build and runtime for the two Node services"
          }
        ],
        "observations": [
          {
            "finding": "CVE-2026-86145, -89156, -89157, -89158, -89160, -89161 (pcre2 before 10.48)",
            "affects": "libpcre2-8-0 10.42-1 in node:24-bookworm-slim, reported by Trivy against apps/data-api and apps/mcp-tools - twelve code-scanning alerts, six CVEs across two images",
            "state": "no patched base image is published anywhere upstream",
            "evidence": [
              "Trivy names the fix as libpcre2-8-0 10.42-1+deb12u1, matching Debian's tracker: bookworm 10.42-1 vulnerable, bookworm-security 10.42-1+deb12u1 fixed under DLA-4772-1.",
              "node:24-bookworm-slim as published (image created 2026-09-09) still carries 10.42-1. Read out of the image's own /var/lib/dpkg/status over the registry API, not inferred from a tag date.",
              "node:24-trixie-slim (also 2026-09-09) carries 10.46-1~deb13u1, and Debian's fixed trixie version is 10.46-1~deb13u2 - so a bookworm-to-trixie swap trades the six known CVEs for the SAME six, plus an untested distro major bump. That is a bump that fixes nothing, and it is worse than the honest state.",
              "debian:bookworm-slim and debian:trixie-slim (2026-08-24) are older still, so there is no fresher upstream to move to.",
              "The three Alpine apps are NOT affected and need no change: nginx:1.31-alpine carries pcre2 10.48-r0, the upstream release that fixes all six. Alpine 3.23 and 3.24 have shipped 10.48-r0 since 2026-09-02 - ahead of the 2026-09-11 CVE publication - and node:24-alpine contains no pcre2 at all."
            ],
            "action": "none, deliberately. The Dockerfiles pull FLOATING base tags, so the next app CI build after Docker republishes node:24-bookworm-slim picks the fix up with no PR and no lag. Pinning a digest here would convert that into a Dependabot round trip for the one CVE set we are waiting on, which is the wrong direction while the fix is the thing in flight.",
            "reviewWhen": "Docker Hub republishes node:24-bookworm-slim after Debian's bookworm-security pcre2 upload. Check with: docker run --rm node:24-bookworm-slim dpkg -s libpcre2-8-0"
          }
        ]
      }
    }
  },

  "notAutomatable": {
    "$comment": [
      "Findings for which NO automated path exists, so V10.1 must not count them as a",
      "draining backlog and V10.4 must not demand an upstream fix for them. Each entry needs",
      "a reason: an unexplained entry is how an exclusion list becomes a place to hide a",
      "finding nobody wants to fix.",
      "",
      "Empty by design. Add an entry only when the class genuinely has no automated fix path,",
      "and say why here rather than in a commit message nobody will find."
    ],
    "ecosystems": [],
    "rules": []
  },

  "excludedPaths": {
    "$comment": [
      "Manifests whose findings are deliberate and are NOT part of the operational backlog.",
      "",
      "apps/vuln-lab holds knowingly-vulnerable pins. Since the 2026-09-05 design it is no",
      "longer load-bearing for any criterion - it is a manual demonstration generator, armed",
      "deliberately by a human when the queue is empty or the chain needs exercising in front",
      "of an audience. Its findings must therefore not age against an SLO the estate never",
      "intended to meet: the package is excluded from the root npm workspaces, is never",
      "imported by any deployed application, and is not built into any container image.",
      "",
      "This is an exclusion from the BACKLOG, not from visibility. The alerts still exist,",
      "V10.3 still proves the surface was readable, and the audit reports how many findings",
      "were excluded and why, so an empty backlog can never be manufactured by adding a path",
      "here unnoticed."
    ],
    "manifests": [
      "apps/vuln-lab/package-lock.json",
      "apps/vuln-lab/package.json"
    ],
    "sourcePrefixes": [
      "apps/vuln-lab/"
    ]
  }
}
