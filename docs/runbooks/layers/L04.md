# L4 Playbook — Purview Sensitivity Labels

| | |
|---|---|
| **Layer** | L4 |
| **Lead** | Identity & Governance |
| **Master plan** | `docs/superpowers/plans/2026-08-22-g1-master-plan.md` § L4 |
| **Teardown gate class** | **G3**; standard cycle: labels persist (brief requirement) |
| **Cost delta** | $0 |

## Purpose

Create the four-level sensitivity taxonomy — **`<prefix>-public` / `<prefix>-internal`
/ `<prefix>-confidential` / `<prefix>-export-controlled`**, where `<prefix>` is
`defaultCompanyPrefix` from `infra/bicep/naming.bicep` (`mls` unless you changed it) —
via Security & Compliance PowerShell, and **publish the label
policy that scopes it to the demo groups**, so the taxonomy is a control a user can
actually apply rather than a set of directory objects nobody can reach (F18). Also
document — design-only, **not applied by any script in this repo** — the auto-label
policy for the lakehouse workspace `mls-operations`; see
`infra/purview/auto-label-design.md` for the design and for why it stays
documented-only. Labels and the policy are tenant-level objects that persist across
every standard kill/rebuild cycle; their GUIDs become fixed audit evidence recorded
at first creation.

## Preconditions

- G0 complete + G1b granted; L3 Verifier PASS.
- Orchestrator unblock for L4.
- **M365 E5 trial active** (g0-bootstrap § B): label creation via S&C PowerShell
  requires the Exchange-backed compliance portal — this is precisely why the dual
  trials are activated the same day the tenant switches on. The M365 E5 trial's
  30-day window means L4 should run early in the tenant's life; the labels persist
  after the trial lapses.
- Credentials: the S&C endpoint accepts app-only auth only for apps granted
  `Exchange.ManageAsApp` plus a compliance admin role. [derived]
  `01-root-oidc.ps1` grants `mls-github-deployer` `Exchange.ManageAsApp` with the
  Compliance Administrator role, and `mls-verifier` `Exchange.ManageAsApp` with the
  **View-Only Configuration** (read-only) role so `Get-Label` works for the audit;
  the master plan is silent on S&C auth plumbing, so this is the minimal-privilege
  derivation.
- Track C artifact merged: `infra/purview/labels.ps1` Pester-green,
  PSScriptAnalyzer-clean.

## Deploy procedure

1. Dispatch:

   ```
   gh workflow run layer-04-purview.yml
   ```

   [derived] Workflow name per the `layer-<nn>-*.yml` pattern. The workflow runs
   `infra/purview/labels.ps1` (ExchangeOnlineManagement /
   `Connect-IPPSSession`), which:
   - creates each of the 4 labels **if absent** (`Get-Label` first — existing labels
     are never recreated, preserving their GUIDs);
   - publishes (or, on replay, updates in place) the single policy
     `mls-demo-label-policy`, naming all four labels and scoped to the four demo
     groups `infra/entra/manifest.json` defines — `mls-flight-operations`,
     `mls-security-team`, `mls-finance`, `mls-executives` — via
     `New-LabelPolicy`/`Set-LabelPolicy` (F18: a label with no published policy
     cannot be applied by anyone and enforces nothing);
   - records the resulting label GUIDs to
     `verification/reports/label-guids.json` via PR (first run only — thereafter the
     file is the fixed expectation the audit compares against).
2. The auto-label policy for `mls-operations` is **documented only**, in
   `infra/purview/auto-label-design.md`. `labels.ps1` does not apply it and has no
   runtime AIP-P2 entitlement check — not because the entitlement check was never
   built, but because there is nothing for it to gate: auto-labeling a Fabric
   lakehouse item is a different Purview Information Protection surface from the
   Exchange/SharePoint/OneDrive/Teams locations `New-AutoSensitivityLabelPolicy`/
   `Set-AutoSensitivityLabelPolicy` can scope to, so this script's S&C session has
   no cmdlet that reaches Fabric regardless of licensing. See
   `auto-label-design.md` for the full reasoning and what a real implementation
   would need.
3. Post run URL to the Verifier.

## Validation cycle

Run by the Verifier as `mls-verifier` via `verification/layer-04-audit.ps1`
(read-only S&C session). Report: `verification/reports/L04-<date>.md`.

### V4.1 — Four labels exist with expected GUIDs

- **Criterion (master plan):** `Get-Label` returns the 4 labels with expected GUIDs
  recorded to `verification/reports/`.
- **Query:**

  ```powershell
  Connect-IPPSSession -AppId $env:MLS_VERIFIER_APP_ID -Organization $env:TENANT_DOMAIN -CertificateThumbprint $env:MLS_VERIFIER_CERT
  Get-Label | Select-Object DisplayName, Guid |
    Where-Object DisplayName -in 'mls-public','mls-internal','mls-confidential','mls-export-controlled'
  ```

- **Expected:** exactly 4 labels — `<prefix>-public`, `<prefix>-internal`,
  `<prefix>-confidential`, `<prefix>-export-controlled`. **First L4 run:** GUIDs are captured and committed to
  `verification/reports/label-guids.json` (the "recorded to `verification/reports/`"
  half of the criterion). **Every subsequent run:** returned GUIDs must equal the
  recorded ones exactly — a changed GUID means a label was deleted and recreated,
  which only a G3 action could legitimately do.
- **Retry/propagation:** label replication across S&C endpoints can lag; standard
  30-minute bounded retry, poll every 5 minutes. (Downstream *client* replication
  can take up to 24 h but is not this criterion's scope.)

### V4.2 — Labels survive a kill/rebuild cycle

- **Criterion (master plan):** labels survive a kill/rebuild cycle (checked again at
  L11).
- **Query:** identical to V4.1, executed in two contexts: (a) at L4 sign-off to
  establish the baseline, and (b) re-executed by the L11 audit immediately after
  `down.ps1` and again after `up.ps1` — L11's "tenant objects intact" criterion
  (V11.2) invokes this check by reference; this playbook owns the criterion, L11
  owns the re-execution schedule.
- **Expected:** same 4 labels, same GUIDs as `label-guids.json`, at every
  checkpoint. Any delta post-`down.ps1` means the teardown path touched tenant
  objects — a critical defect in `down.ps1`, stop-the-line.
- **Retry/propagation:** none beyond V4.1's window — persistence is binary.

### V4.3 — Label policy exists and is scoped to the demo groups

- **Criterion:** NOT a master-plan criterion — it is not in the 43-row
  traceability table in `docs/runbooks/layers/README.md`, which that file's own
  header states is scoped exactly to master-plan criteria. Added closing F18
  (Task 20 — CM-6): `infra/purview/labels.ps1` created the four-label taxonomy but
  never published a policy scoping it to anyone, so V4.1/V4.2 — which only ever
  check label existence and GUID survival — reported L4 healthy while the labels
  were unpublished and unusable. This is the supplementary policy check Failure
  mode 5 (below) already anticipated, now implemented.
- **Query:**

  ```powershell
  Connect-IPPSSession -AppId $env:MLS_VERIFIER_APP_ID -Organization $env:TENANT_DOMAIN -CertificateThumbprint $env:MLS_VERIFIER_CERT
  Get-LabelPolicy -Identity 'mls-demo-label-policy' | Select-Object Labels, ExchangeLocation
  ```

- **Expected:** the policy exists; `Labels` == `<prefix>-public`, `<prefix>-internal`,
  `<prefix>-confidential`, `<prefix>-export-controlled` (exactly); `ExchangeLocation` == `mls-flight-operations`,
  `mls-security-team`, `mls-finance`, `mls-executives` (exactly). Any missing or
  extra label or group is a FAIL — same treatment V4.1 gives a missing or extra
  label in the taxonomy itself.
- **Retry/propagation:** same 30-minute bounded retry as V4.1 — policy scoping can
  lag replication the same way label creation does.

## Teardown

- **Standard kill/rebuild cycle:** **labels persist — the cycle must not touch
  them** (brief principle 2 and spec F6). `infra/purview/labels.ps1` replayed by
  `up.ps1` no-ops via its create-if-absent guard.
- **Full teardown:** G3-gated script `infra/purview/teardown.ps1` [derived name —
  master plan says "G3-gated script" without a path; placed beside the apply script
  per repo convention] removes the label policy then the labels. **Gate class: G3.**
  Note the honest consequence: recreated labels get new GUIDs;
  `label-guids.json` must be re-baselined in the same PR that records the G3
  approval.

## Rollback

- **First validation failure:** Verifier files specifics. Lead fixes
  `infra/purview/labels.ps1` or the label policy via PR and re-dispatches; the
  create-if-absent design means re-runs converge without deleting anything.
  **Never delete a label to fix it** — deletion is G3 and orphans the recorded
  GUIDs; label properties (tooltip, policy scope) are all modifiable in place.
- **If V4.1 fails on GUID mismatch:** do not "fix" by re-baselining the JSON — a
  GUID change without a recorded G3 approval is an incident. Escalate to the
  Orchestrator with the S&C audit log (`Search-UnifiedAuditLog -Operations
  DeleteLabel`) before any remediation.
- **Second consecutive failure of L4 → G4.**

## Failure modes

1. **S&C session fails to establish** (`Connect-IPPSSession` errors — tenant not
   Exchange-backed, trial not active, or role missing). Detection: workflow fails at
   connect. Response: verify M365 E5 trial state (human, admin center); verify the
   `Exchange.ManageAsApp` grant + role assignment from G0. This is the spec F1
   licensing risk landing — degrade path (documented-design-only) requires sponsor
   sign-off, never a silent skip.
2. **Label GUID drift between rebuilds.** Detection: V4.1 GUID mismatch. Response:
   treat as incident (see Rollback); root causes seen in the wild: someone ran a G3
   teardown without re-baselining, or a portal-side manual deletion (violates
   principle 1 — zero manual portal actions after G0).
3. **M365 E5 trial lapse (day 30) before first L4 run.** Detection:
   `New-Label` rejected for licensing. Response: labels already created persist and
   remain manageable; if L4 never ran, sponsor decision — re-trial path/paid, or the
   spec F1 descope. Schedule L4 in the first week of tenant life to avoid this.
4. **Auto-labeling is documented-only; this is not a deploy-time failure mode.**
   There is no runtime entitlement check and no apply path in `labels.ps1` for it
   (Deploy procedure step 2; `infra/purview/auto-label-design.md`) — not a
   licensing degrade, a mechanism gap: the S&C cmdlets `labels.ps1` can call
   (`New-AutoSensitivityLabelPolicy`/`Set-AutoSensitivityLabelPolicy`) have no
   Fabric location type to scope to. Nothing here can fail at deploy time because
   nothing here runs. This item is kept in the list only so a future contributor
   who builds a real Fabric-native apply path (see `auto-label-design.md` §"If
   this is ever built") knows to add a matching failure mode of their own.
5. **Label policy scoping error** (labels published to the wrong groups, or to
   none, invisible in client apps). Detection: not caught by V4.1 (the four labels
   exist regardless of who they're published to); caught by **V4.3**, the
   supplementary policy-scope check (`Get-LabelPolicy | Select -Expand
   ExchangeLocation` — not a master-plan criterion, so it is not in
   `docs/runbooks/layers/README.md`'s traceability table, same convention CP-9's
   V6.5 uses). Response: fix policy scope in `labels.ps1` (`Get-LabelPolicyScope`),
   re-apply — the create-if-absent/update-on-drift design means a re-run converges
   without deleting and recreating the policy.

## Deferred validation

- **Pre-validated locally in Phase P (Track C), extended Task 20 (F18):**
  `infra/purview/labels.ps1` under Pester with mocked S&C cmdlets — create-if-absent
  guard (the GUID-preservation property), taxonomy content (exactly the 4 names),
  error paths, and (added Task 20) the label policy publish step: create-if-absent,
  update-in-place on label-list or scope drift, scoped to exactly the four demo
  groups, `-WhatIf` makes no mutating policy calls either; PSScriptAnalyzer clean;
  `actionlint` on `layer-04-purview.yml` (Track G).
- **Deferred until tenant (named explicitly):** live label creation and all three
  criteria — V4.1 (`Get-Label` + GUID recording; no S&C endpoint exists to query
  pre-tenant), V4.2 (survival across a cycle; requires L11's live down/up), and V4.3
  (the policy's real `ExchangeLocation` scoping, which only a live tenant can
  confirm actually reaches the named groups). Covered by
  `verification/layer-04-audit.ps1` at L4, V4.2 re-invoked by
  `verification/layer-11-audit.ps1`.
